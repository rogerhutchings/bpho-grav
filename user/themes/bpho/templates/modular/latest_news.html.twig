{% embed 'partials/polygon.html.twig' %}
  {% set posts = page.find('/blog').children.order('date', 'desc').slice(0, 5) %}

  {% block title %}
    {{ header.title }}
  {% endblock %}

  {% block content %}
    <ul class="list pl0 ma0">

      {% for post in posts %}
        <li class="bt b--moon-gray flex pt2 mb3">

          <time class="db w-20" datetime="{{ post.date|date("c") }}">
            <span class="f2 db lh-solid">
              {{ post.date|date('d') }}
            </span>
            <span class="f6 db">
              {{ post.date|date('F') }}
            </span>
          </time>

          <div class="w-80">
            <a href="{{ post.url }}" class="db black underline-hover">
              {{ post.title }}
            </a>
            <a href="{{ post.url }}" class="db tr mt2 underline-hover fw2">
              Read more...
            </a>
          </div>

        </li>
      {% endfor %}

    </ul>
  {% endblock content %}
{% endembed %}
